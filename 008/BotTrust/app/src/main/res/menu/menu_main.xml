<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="org.tbadg.bottrust.MainActivity">

    <group
        android:id="@+id/group"
        android:checkableBehavior="single">

        <item
            android:id="@+id/action_small_set"
            android:checked="true"
            android:orderInCategory="100"
            android:title="@string/action_small_set"
            app:showAsAction="never"/>

        <item
            android:id="@+id/action_large_set"
            android:checked="false"
            android:orderInCategory="110"
            android:title="@string/action_large_set"
            app:showAsAction="never"/>
    </group>

    <item
        android:id="@+id/action_sim_all"
        android:checkable="true"
        android:checked="false"
        android:orderInCategory="120"
        android:title="@string/action_sim_all"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_quick"
        android:checkable="true"
        android:checked="false"
        android:orderInCategory="130"
        android:title="@string/action_quick"
        app:showAsAction="never"/>
</menu>
